{
  "name": "se2-ticketing-platform-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Lightweight TypeScript backend skeleton for the SE2 ticketing platform project.",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.7",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
